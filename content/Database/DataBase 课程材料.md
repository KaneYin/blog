# 第一章概述

## 什么是数据库，什么是数据库管理系统

**数据库**: 存储数据的仓库 (DataBase DB)，相互之间有关联关系的表的集合。
**数据库管理系统**: 操作和管理数据库的大型软件 (DBMS) 比如MySQL数据库就是数据库管理软件。数据库管理系统是一种**系统软件**
**SQL**: 操作关系型数据库的编程语言, 定义了一套操作关系型数据库的统一标准

**本机的MySQL账户ROOT密码为:123456**


## 什么是数据模型

**数据库系统的核心和基础是数据模型**。一般来说，数据模型是严格定义的一组概念的集合。这些概念精确地描述了系统的静态特征、动态特征和完整性约束条件。因此**数据模型一般由数据结构、数据操作和完整性约束三部分组成**

1. **数据结构**：存储在数据库中对象类型的集合，作用是描述数据库组成对象以及对象之间的联系
2. **数据操作**：指对数据库中各种对象实例允许执行的操作的集合，包括操作及其相关的操作规则
3. **完整性约束**：指在给定的数据模型中，数据及其联系所遵守的一组通用的完整性规则，它能保证数据的正确性和一致性

根据模型应用目的的不同，数据模型分为2类：

* 第一类
    - 1）**概念模型**：也称为信息模型。它是按用户的观点来对数据和信息建模，主要用于数据库设计
* 第二类
    - 2）**逻辑模型**：主要包括层次模型、网状模型、**关系模型**、面向对象模型和对象关系模型等
    - 3）**物理模型**：是对数据最底层的抽象，它描述数据在系统内部的表示方法和存取方法，在磁盘或磁带上的存储方式和存取方法，是面向计算机系统的
    

**关系模型是目前最重要的一种数据类型**。关系数据库系统采用关系模型作为数据的组织方式

* 关系模型中数据的逻辑结构是一张*二维表*，或者说关系的数据结构就是一张表
* 关系数据模型的数据操作主要包含*查询*、*插入*、*删除*和*更新*数据
* 关系模型的完整性约束条件包含三大类：*实体完整性*、*参照完整性*和*用户自定义的完整性*
    - 关系模型的*实体完整性规则*：若属性（指一个或一组属性）A是基本关系R的主属性，则A不能取空值（由此规则可得一直接结论：主键不能为空）
    - 关系模型的*参照完整性规则*：若属性（或属性组）F是某基本关系R的外键，且它与基本关系R1的主键相对应，则对于R中，每个F上的值或为空值或者等于R1中的主键值

## 研讨作业
### 研讨1-1：
为什么要学习数据库系统课程？数据库与文件系统相比有什么差异？ 请分享你的观点

学习本课程，一方面，能够使我们了解“数据管理”的发展过程；掌握目前“数据库系统”的相关基本概念、基本理论和管理方法；学习数据库引用程序的构成和开发途径、方法、工具。另一方面，为专业的后继课程提供必要的技术基础。

差异：
**文件系统**面向某一应用程序，共享性差，冗余度大，数据独立性差，由应用程序自己控制。**数据库系统**面向现实世界，共享性高，冗余度小，具有较高的物理独立性和一定的逻辑独立性，整体结构化，用数据模型描述，由数据库系统提供数据安全性，完整性，并发控制和恢复力。
### 研讨1-2：
数据库、数据库系统、数据库管理系统之间的关系是什么？

数据库系统是一种包含软件，硬件，使用人员的一种数据库框架。数据库系统包含三层结构，数据库位于三层结构中最下面的一层；数据库管理系统位于三层结构中的中间层，主要用于建立、使用和维护数据库，是位于用户程序和操作系统之间的专用软件；数据库应用程序位于三层结构中的最上层，主要是提供给非专业人员使用数据库。

### 研讨1-3：
数据库的三级模式两级映射指什么？定义它的作用是什么？它处在数据库系统框架结构的哪一层中？

三级模式分别指：外模式，模式，内模式。
两级映射分别指：外模式/模式映射，模式/内模式映射。
定义它的作用：为了减少数据冗余，实现数据共享，提高数据的独立性；为了提高存取效率，改善性能
它处在数据库系统框架结构中的中间层。

### 研讨1-4：
第1章学习后,能否尝试以知识图谱的形式将你学习到的基本概念、数据库相关的结构、以及数据模型等关联表达出来，咱们做个本章小节分享。知识图谱可以图片形式提交，注意上传图片的方向。

![[数据库系统(DBS)组成.png]]

# 第二章 关系数据模型与关系代数

## 第二章单元测试

### 3. (Single Choice)以下对关系模型(也称关系模式)和关系的描述中，正确的是(   )。

- A. 关系模式是关系的结构, 关系是关系模式在某一时刻的数据
- B. 同一关系模式下，可有很多的关系
- C. 关系模式是稳定的，而关系是某一时刻的值，是随时间可能变化的
- D. 其余3项都正确

_Correct Answer:_ D
### 13. (Single Choice)关系数据模型是目前最重要的一种数据模型，它的三个要素分别是( B )。

- B. 数据结构、关系操作、完整性约束
### 10.关系代数运算是以( B )为基础的运算。其基本操作是并、差、笛卡尔积,投影和选择。

- B. 关系运算
### 15.四元关系R的属性为A、B、C、D，若R含有100个元组，则 $π_B,_C(R)$ 含有( D )个元组。

- D. 100

>投影运算**不会去重**，只有SQL语句中使用 `select distinct B,C` 才会去掉重复的元组

## 第二章线上作业

## 补充内容
### 关系与关系模式的区别与联系

#### 关系
$D1 × D2 ×...× Dn$ 的子集叫做在域 $D1，D2，...，Dn$ 上的关系，表示为 $R(D1，D2，...，Dn )$，其中 $R$ 就是关系名，$n$ 就是目或度。  
一个关系对应一张二维表（Table），二维表名就是关系名。
#### 关系模型
关系模式是对关系的描述。**SQL语言**就是建立在关系模型的基础上的。
一个关系就是一个Table，关系模型就是处理关系的，主要由以下三部分构成：
- 描述DB各种数据的基本结构形式 **(Table)**
- 描述Table与Table之间可能发生的各种操作 **(关系运算)**
- 描述这些操作应遵循的约束条件 **(完整性约束)**

**关系运算：交、并、差、补、选择、投影、连接、除**
**完整性约束：实体完整性，参数完整性，用户自定义的完整性**
#### 区别
- **关系**是**关系模式**在某一时刻的状态或内容。
- **关系模式**是静态的；**关系**是动态的。
- **关系模式**是二维表的表头属性等，即一个二维表的主要架构。由于二维表的属性名一般不会修改，所以呈现出静态。
- **关系**是一张二维表的具体数据，除去表头外各数据间的联系。由于二维表中是数据会时常修改，所以呈现出动态。

### 超建、候选键、主键、外键
**超键**(super key):在关系中能唯一标识元组的属性集称为关系模式的超键

**候选键**(candidate key):不含有多余属性的超键称为候选键

**主键**(primary key):用户选作元组标识的一个候选键程序主键

**外键**(foreign key):如果关系模式R1中的某属性集不是R1的主键，而是另一个关系R2的主键则该属性集是关系模式R1的外键。

[菜鸟学数据库（四）——超键、候选键、主键、外键](https://zhuanlan.zhihu.com/p/33394962)


# 第三章 SQL语法

课程中重点学习的select语句属于DQL (数据查询语句)
SQL基础语法参见 [[MySQL#基础篇]]
## 研讨题
### 研讨3-5：请问从CAP数据库中, 该怎样用SQL语句表达如下查询。(1)查姓张的顾客；(2)查名字中包含‘英’字的顾客。

(1) 查找姓张的顾客: select * from customers where cname like '张%';
(2) 查找名字中包含‘英’字的顾客: select * from customers where cname like '%英%';

### 课后研讨3-4：
已知关系模式：S(Sno,Sname,Sclass)，C(Cno,Cname,Cteacher)，SC(Sno,Cno,Scgrade)。其中，S为学生关系：Sno学号, Sname姓名,Sclass班级； C为课程关系：Cno课程号, Cname课程名, Cteacher任课教师；SC为学生选课关系：Scgrade成绩。你能给出实施下列查询目标的思路、及其SQL句吗？ (1)查学过“John”老师讲授课程的所有学生。 
(2)查学全了“Mary”老师讲授课程的所有学生； 
(3)查没学过“李明”老师讲授课程的所有学生。 
(4)查没学全“李明”老师讲授课程的所有学生。针对这三个关系，你认为还有什么查询比较难呢，请你试试出些题目吧？

(1) 查学过“John”老师讲授课程的所有学生: 
select distinct S.Sno, S.Sname from S, SC, C where S.Sno = SC. Sno and C.Cno = SC. Cno and C.Cteacher = 'John';

(2) 查学全了 "Mary"老师讲授课程的所有学生: 也就是查找不存在一个学生没有学过"Mary"老师的讲授课程.
```SQL
select distinct S.Sno, S.Sname from S, SC, C where not exists (select * from S, SC, C where C.Cteacher = 'Mary' and not exists (select * from S, SC, C where S.Sno = SC. Sno and C.Cno = SC. Cno));
```

(3) 查没有学过“李明”老师讲授课程的所有学生

```SQL
select distinct S.Sno, S.Sname  from S, SC, C where not exists(select * from S, SC, C where C.Cteacher = '李明' and S.Sno = SC.Sno and SC.Cno = C.Cno);
```

(4) 查没学全“李明”老师讲授课程的所有学生: 可以使用学过“李明”老师讲授课程的所有学生减去学全“李明”老师讲授课程的所有学生, 即可得到没有学全“李明”老师讲授课程的所有学生

```SQl
select distinct * from S, SC, C where S.Sno = SC.Sno and SC.Cno = C.Cno and not exists (select * from S, SC, C where C.Cteacher = 'Mary' and not exists (select * from S, SC, C where S.Sno = SC. Sno and C.Cno = SC. Cno)))
```

(5) 查询没有学过所有老师, 所有课程的学生


### 左外连接和右外连接的区别

**内连接：相当于保留两个表之间交集的部分
![[内连接示意图.jpg]]

**外连接：相当于保留两个表之间并集的部分，又叫全外连接
![[外连接示意图.jpg]]
**左外连接：相当于保留两个表交集部分加上左表的部分
![[左外连接示意图.jpg]]
**右外连接：相当于保留两个表交集部分加上右表的部分
![[右外连接示意图.jpg]]

## 线上作业

### 使用MySQL创建关系表
存在如下学生S、课程C、学生选课SC关系，请在某个DBMS软件平台下完成下列工作

(1)设计这一关系型数据库，包含合理的表名、列名、数据类型、列级约束、表级约束。写出设计文档。
(2)编写创建所设计关系表的SQL语句；插入数据记录的SQL语句。并附上你的运行结果截图。

```SQL
-- 1. 创建学生表-选课表-课程表  
-- S 学生表（ 学号Sno, 姓名Sname, 性别Sex, 年龄Age）  
-- C 课程信息表（课号Cno, 课程名称Cname, 授课教师Teacher）  
-- SC 选课表（学号Sno, 课号Cno, 成绩Grade）  
create database SC;  
create table S  
(  
    Sno char(5) not null,  
    Sname varchar(10),  
    Sex char(2),  
    Age tinyint unsigned,  
    primary key (Sno)  
);  
  
create table C  
(  
    Cno char(5) not null,  
    Cname varchar(10),  
    Teacher varchar(10),  
    primary key (Cno)  
);  
  
create table SC  
(  
    Sno char(5) not null,  
    Cno char(5) not null,  
    Grade double(4,1),  
    foreign key (Sno) references S(Sno),  
    foreign key (Cno) references C(Cno)  
);  
  
-- 更改字符集  
alter table S default character set utf8;  
alter table S change Sex sex varchar(40) character set utf8;  
alter table S change Sname Sname varchar(40) character set utf8;  
alter table S change Age Age varchar(40) character set utf8;  
  
-- 2.向数据库中插入数据  
insert into S values ('s001','李勇','男',20),  
                     ('s002','刘晨','女',20),  
                     ('s003','王敏','女',21),  
                     ('s004','张立','男',18),  
                     ('s005','张三','男',19),  
                     ('s006','李四','女',20);  
  
-- 更改数据库sc的默认字符集,更改表C Cname列的字符集(因为这一列要输入中文)  
alter database sc character set utf8mb4;  
alter table C character set utf8mb4;  
alter table C change Cname Cname varchar(20) character set utf8;  
  
-- C 课程信息表（课号Cno, 课程名称Cname, 授课教师Teacher）  
insert into C values ('c01','数据结构','WangMing'),  
                     ('c02','数学','WangMing'),  
                     ('c03','数据库','HePing'),  
                     ('c04','操作系统','LiQiang'),  
                     ('c05','C语言','LiQiang');  
-- SC 选课表（学号Sno, 课号Cno, 成绩Grade）  
insert into sc values ('s001','c01','80'),  
                      ('s001','c02','85'),  
                      ('s001','c05','78'),  
                      ('s002','c03','88'),  
                      ('s002','c04','81'),  
                      ('s003','c01','64'),  
                      ('s003','c03','70'),  
                      ('s004','c05','90'),  
                      ('s005','c02','98'),  
                      ('s006','c04','76');
```

```sql
-- (1)查询每门课程的课号、课名、选课人数及平均成绩；要求查询结果按平均成绩的降序排列。  
select sc.Cno,Cname,count(*) as '课程选课人数',avg(Grade) as '课程平均成绩' from SC,C where C.Cno=SC.Cno group by C.Cno ORDER BY 4 DESC;  
  
-- (2)生成每门课程的课号、选课男生人数及平均成绩、选课女生人数及平均成绩。  
select sc.Cno,S.sex,count(*) as '选课人数',avg(Grade) as '平均成绩' from sc,c,s where sc.Cno = c.Cno and s.Sno = sc.Sno group by C.Cno,s.Sex;  
  
-- 统计每位学生的选课门次、平均成绩，要求按“学号、姓名、学科门数、平均成绩”列输出成绩统计表，表中应按“平均成绩”的降序排列数据。  
select s.Sno,s.Sname,count(C.Cno) as '学科门数',avg(Grade) as '平均成绩' from c,sc,s where c.Cno = sc.Cno and s.Sno = sc.Sno group by S.Sno order by 4 DESC;  
  
-- 请从中检索出选修了课程名含“数据”的同学，将他们的选课成绩置为空值  
UPDATE sc SET Grade = NULL WHERE sc.Sno IN (  
    SELECT temp.Sno  
    FROM (SELECT S.Sno FROM c, sc, s WHERE c.Cno = sc.Cno AND s.Sno = sc.Sno AND C.Cname LIKE '%数据%'  
    ) AS temp  
);  
  
 SELECT * FROM c, sc, s WHERE c.Cno = sc.Cno AND s.Sno = sc.Sno AND C.Cname LIKE '%数据%';  
  
-- 在选课记录表中添加一条同时选了 "LiQiang" 老师所开全部课程的学生  
insert into sc values ('s006','c05',68);  
  
-- 查选修了“Li Qiang”老师所开全部课程的学生学号、姓名、平均成绩  
-- 即查询不存在没有选修“Li Qiang”老师所开全部课程的学生学号、姓名、平均成绩  
    select s.Sno,s.Sname,avg(sc.grade) from s,sc where s.Sno = sc.Sno and  
    not exists(select * from  c where c.Teacher = 'LiQiang' and  
    not exists(select * from sc where c.Cno = sc.Cno and s.Sno = sc.Sno));  
  
-- 查询选修了全部课程的学生姓名  
    select Sname from s where not exists(  
        select * from c where not exists(  
            select * from sc where S.Sno=sc.Sno and C.Cno=sc.Cno  
        )  
    );  
  
-- 要查未选“C语言、数据库“的学生学号和姓名，请阐述数据查询思路，写出SQL查询语句。  
-- 可以先查找学过C语言或者学过数据库的学生信息,然后用子查询选出不在这些结果里面的学生信息,即是即没学过C语言也没学过数据库的学生信息  
SELECT s.Sno, s.Sname FROM s WHERE NOT EXISTS (  
  SELECT * FROM sc INNER JOIN c ON c.Cno = sc.Cno WHERE s.Sno = sc.Sno  
    AND (c.Cname = 'C语言' OR c.Cname = '数据库')  
);
```
### 问题汇总

1. Mysql插入中文报错：ERROR 1366 (HY000): Incorrect string value...

解决方式：将表以及字段的字符集改为“utf8mb4”
**查看数据表的编码：**

```SQl
 show create table Student; 
```

 **修改表的编码方式：**

```SQL
 alter table Student default character set utf8;
```

**修改字段的编码方式：**

```
 alter table Student change Sname Sname varchar(20) character set utf8mb4;
```

2. 报错：Column ‘xxx‘ in field list is ambiguous
字段中的某一列含义不明确，这是因为同一数据库中的不同表之间含有相同的字段名，并且同时出现在了SQL语句中，导致的语法错误，需要为同名的字段加上表名。
[完美解决Column ‘xxx‘ in field list is ambiguous问题](https://blog.csdn.net/Andrew_Chenwq/article/details/129253748)

## 第三章线上测试

第四题
![[Database线上测试-4.png]]
>在MySQL中，**任何列都可以设置为允许为NULL或不允许为NULL**。如果将列设置为允许为NULL，则该列可以存储NULL值。如果将列设置为不允许为NULL，则该列不能存储NULL值。因此，如果要允许INT类型的列存储NULL值，则可以将该列设置为允许为NULL。

 但是，需要注意的是，**INT类型列的默认值不能是NULL。如果要在新记录中将INT类型的列设置为NULL，则必须将该列设置为允许为NULL，并将其显式地设置为NULL值。**

例子:
```SQL
CREATE TABLE `example_table` (
    `id` INT(11) NULL,
    `name` VARCHAR(255) NOT NULL
);
```



# 第六章数据库设计

## 规范化设计理论
规范化设计是要解决:**如何构造合适的数据结构**

### 不好的关系模式具有以下问题
- 信息数据冗余太大
- 插入异常
- 删除异常
- 更新异常

## 数据库设计过程（一共六个阶段）

这是一个完整的实际数据库及其应用系统的设计过程。不仅包括设计数据库本身，还包括数据库的实施、数据库运行和维护。
**需求分析**
准确了解与分析用户需求（包括数据与处理）。
**概念结构设计**
通过对用户需求进行综合、归纳与抽象，形成一个独立于具体DBMS的概念模型。
**逻辑结构设计**
将概念结构转换为某个DBMS所支持的数据模型，并对其进行优化。
**数据库物理设计**
为逻辑数据模型选取一个最适合应用环境的物理结构（包括存储结构和存取方法）
**数据库实施**
设计人员运用DBMS提供的数据语言、工具及宿主语言，根据逻辑设计和物理设计的结果建立数据库，编制与调试应用程序，组织数据入库，并进行试运行。
**数据库运行和维护**
在数据库系统运行过程中对其进行评价、调整与修改。

## 数据模型与概念模型
表达计算机世界的模型称为**数据模型**，表达信息世界的模型称为概念数据模型，简称为**概念模型**，信息世界是对现实世界的抽象。概念模型有E-R模型，数据模型有：关系模型、层次模型、网状模型。

![[数据建模与数据库设计在《数据库系统》中的位置.png]]

![[数据模型与概念模型的层次结构.png]]

数据建模是抽象，抽象是理解-区分-命名-表达


## 实体与实例
实体：客观存在并可互相区分的事物，实体用**属性**来刻画。属性是实体所具有的某一方面的特性。
实体的个体（实体的值）：就是实例，实例用实体的值来刻画。实例使用关键字（码）来进行区分


## E-R模型
E-R图为实体-联系图，提供了表示实体型、属性和联系的方法，用来描述现实世界的概念模型。
### 基本概念
- 实体
- 属性
- 联系 (以二元联系为例)
	- 一对一联系: ==实体A的一个实例只能和实体B的一个实例发生联系, 反之实体B的实例也只能和实体A的一个实例发生联系。
	- 一对多联系：==实体A的实例能和实体B的多个实例发生联系，反之，实体B的实例只能和实体A的一个实例发生联系。
	- 多对多联系：实体A的一个实例可以和实体B的多个实例发生联系
- 关键字、码
### 表示方法
构成E-R图的基本要素是实体型、属性和联系，其表示方法为：
- 实体型：用矩形表示，矩形框内写明实体名；
- 属性：用椭圆形表示，并用无向边将其与相应的实体连接起来；
- 联系：用菱形表示，菱形框内写明联系名，并用无向边分别与有关实体连接起来，同时在无向边旁标上联系的类型（1 : 1，1 : n或m : n）
### 联系的基数
概念：实体实例之间联系的数量，即一个实体的实例通过一个联系能与另一个实体中相关联的实例的数目。
**最小基数：**
**最大基数：**



## E-R图向关系模型转换的规则
一个实体型转换为一个关系模式。实体的属性就是关系的属性，实体的码就是关系的码。对于实体间的联系则有以下不同的情况：
### 1:1联系
一个1：1联系可以转换为一个独立的关系模式，也可以与任意一端对应的关系模式合并。如果转换为一个独立的关系模式，则与该联系相连的各实体的码以及联系本身的属性均转换为关系的属性，每个实体的码均是该关系的候选码。如果与某一端实体对应的关系模式合并，则需要在该关系模式的属性中加入另一个关系模式的码和联系本身的属性。
### 1: n 联系
一个1：n联系可以转换为一个独立的关系模式，也可以与n端对应的关系模式合并。如果转换为一个独立的关系模式，则与该联系相连的各实体的码以及联系本身的属性均转换为关系的属性，而关系的码为n端实体的码。
### m: n 联系
一个m：n联系转换为一个关系模式。与该联系相连的各实体的码以及联系本身的属性均转换为关系的属性，各实体码的组合组成该关系的码，或码的一部分。

三个或三个以上实体间的一个多元联系可以转换为一个关系模式。与该多元联系相连的各实体的码以及联系本身的属性均转换为关系的属性，而关系的码为各实体码的组合。
具有相同码的关系模式可合并。



## 第六章单元测试
### 题1 设E-R图由3个实体、一个m:n联系和一个1:n联系构成，则根据E-R图向关系模型转换的规则，转换得到的关系模型应包含（ ）个关系模型。

### 题2 从ER图导出关系模型时，如果实体间的联系是M：N的，下列说法中正确的是【增加一个关系表示联系，其中纳入M方和N方的关键字】

### 题3  在ER模型中，如果有3个不同的实体集，3个M：N联系，根据ER模型转换为关系模型的规则，转换为关系的数目是【6】

### 数据库外模式在【数据库逻辑结构设计】阶段设计

### 数据库设计中，用E－R图赖描述信息结构但不涉及信息在计算机中的表示，这是数据库设计的【概念设计阶段】

### 在数据库设计中，将E-R图转换成关系数据模型的过程属于【逻辑设计阶段】

### 关系数据库的规范化理论主要解决的问题是【如何构造合适的数据逻辑结构】

# 第七章单元测试_关系完整性和安全性
## 把对关系SC中，属性GRADE的修改权授予用户ZHAO，这个SQL语句是 ( )。
D. GRANT UPDATE (GRADE) ON SC TO ZHAO

> grant update (哪种操作) (具体哪一列) **on** table (表的名称) **to** user (用户名称)

## 数据库的（    ）是指数据的正确性和相容性。

A. 完整性
## 有”学生 (学号, 姓名, 系别)”关系,规定学号的值域是8个数字组成的字符串,这一规则属于 ( )。

B. 用户自定义完整性约束

## SQL语言的GRANT和REVOKE语句主要是用来维护数据库的_________

B. 安全性

## 数据库安全审计系统提供了一种 (   )的安全机制。

C. 事后检查

> 概念, 背下来


## 关于“约束”叙述正确的是 ( )。A
A. 以上都不对
B. 每个约束可以作用到多个表的多个列
C. 每个约束可以作用多个列, 但是必须在一个表里
D. 每个约束只能作用于一个列上

## 视图是定义在__或__之上的

基表视图

# create table语句的作用
- 定义关系模式
* 定义安全性约束
* 定义完整性约束

