线程是操作系统中能够进行运算调度的最小单元，它被包含在进程之中，是进程中的实际运作单位。进程是程序的基本执行实体。线程简单理解，就是应用软件中相互独立，可以同时运行的功能。
并发：在同一时刻，有多个指令在单个 CPU 上交替执行。
并行：在同一时刻，有多个指令哎多个 CPU 上同时执行。
# 多线程的实现方式
## 继承 Thread 类的方式进行实现
```java

```

## 实现 Runnable 接口的方式进行实现
```java

```



## 利用 Callable 接口和 Future 接口方式实现
```java

```


## 多线程三种实现方式对比

|                | 优点                         | 缺点                         |
| -------------- | -------------------------- | -------------------------- |
| 继承 Thread 类    | 编程比较简单，可以直接使用 Thread 类中的方法 | 可拓展性较差，不能再继承其他的类           |
| 实现 Runnable 接口 | 拓展性强，实现该接口的同时还可以继承其他的类     | 编程相对复杂，不能直接使用 Thread 类中的方法 |
| 实现 Callable 接口 | 拓展性强，实现该接口的同时还可以继承其他的类     | 编程相对复杂，不能直接使用 Thread 类中的方法 |
# 常见的成员方法

| 方法名称                             | 说明                   |
| -------------------------------- | -------------------- |
| String getName()                 | 返回此线程的名称             |
| void setName(String name)        | 设置线程的名字（构造方法也可以设置名字） |
| static Thread currentThread()    | 获取当前线程的对象            |
| static void sleep(long time)     | 让线程休眠指定的时间，**单位为毫秒** |
| setPriority(int newPriority)     | 设置线程的优先级             |
| final int getPriority()          | 获取线程的优先级             |
| final void setDaemon(boolean on) | 设置为守护线程              |
| public static void yield()       | 出让线程、礼让线程            |
| public static void join()        | 插入线程、插队线程            |

当 JVM 虚拟机启动以后，会自动的启动多条线程，其中有一条线程就是 main 线程，作用就是调用 main 方法，并执行里面的代码。在 JavaSE 阶段写的所有方法，其实都是运行在 main 线程中的。

- [ ] 操作系统中线程的调度方式

